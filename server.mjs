// server.mjs
import express from "express";

// instance of express
const app = express();
const PORT = 3000;

// Middleware

// Routes

// initialize
app.listen(PORT, () => {
  console.log(`Server is running on Port: ${PORT}`);
});
